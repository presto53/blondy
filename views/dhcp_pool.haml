%h1.page-header Dhcp pools configs
.table-responsive
  %table.table.table-striped.table-condensed.table-hover{id: "dhcp_poolConfigs"}
    %thead
      %tr
        %th
          %a.btn.btn-default.btn-primary{href: '/dhcp_pool/create'}
            %span.glyphicon-plus-sign.glyphicon
            New
      %tr
        %th Dhcp pool
    %tbody{id: "list"}
      - if @dhcp_pools
        - @dhcp_pools.each do |dhcp_pool|
          %tr
            %td
              %a{href: "/dhcp_pool/#{dhcp_pool.id}"}= dhcp_pool.name
