!!!
%html{lang: "en"}
  %head
    %meta{charset: "utf-8"}/
    %meta{content: "IE=edge", "http-equiv" => "X-UA-Compatible"}/
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %meta{content: "", name: "description"}/
    %meta{content: "", name: "author"}/
    %title Blondy
    / Bootstrap core CSS
    %link{href: "#{base_url}/css/bootstrap.css", rel: "stylesheet"}/
    / Custom styles for this template
    %link{href: "#{base_url}/css/dashboard.css", rel: "stylesheet"}/
    / Blondy custom styles
    %link{href: "#{base_url}/css/blondy.css", rel: "stylesheet"}/
    / Just for debugging purposes. Don't actually copy this line!
    /[if lt IE 9] <script src="../../docs-assets/js/ie8-responsive-file-warning.js"></script>
    / HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries
    /[if lt IE 9]
      <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
      <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
  %body
    .navbar.navbar-inverse.navbar-fixed-top{role: "navigation"}
      .container-fluid
        .navbar-header
          %button.navbar-toggle{"data-target" => ".navbar-collapse", "data-toggle" => "collapse", type: "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{href: "#"} Blondy
        .navbar-collapse.collapse
          - if session[:logged_in]
            %ul.nav.navbar-nav
              %li
                %a{href: "/logout"} logout
    .container-fluid
      .row
        .col-sm-3.sidebar
          - if session[:logged_in]
            =haml :nav
        .col-sm-9.col-sm-offset-3.main
          - if flash[:error]
            .alert.alert-danger
              :markdown
                **Oops!**
              - flash[:error].each do |name, alert|
                = "#{name.to_s.capitalize} #{alert[0]}<br>"
          =yield
    /
      Bootstrap core JavaScript
      \==================================================
    / Placed at the end of the document so the pages load faster
    %script{src: "https://code.jquery.com/jquery-1.10.2.min.js"}
    -#%script{type: "application/dart", src:"#{base_url}/scripts/blondy.dart"}
    -#%script{src: "#{base_url}/scripts/dart.js"}
    -#%script{src: "../../dist/js/bootstrap.min.js"}
    -#%script{src: "../../docs-assets/js/holder.js"}
