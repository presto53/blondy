%h1.page-header Dhcp configs
.table-responsive
  %table.table.table-striped.table-condensed.table-hover{id: "dhcpConfigs"}
    %thead
      %tr
        %th
          %a.btn.btn-default.btn-primary{href: '/dhcp/create'}
            %span.glyphicon-plus-sign.glyphicon
            New
      %tr
        %th Config name
    %tbody{id: "list"}
      - if @configs
        - @configs.each do |config|
          %tr
            %td
              %a{href: "/dhcp/#{config.id}"}= config.name
